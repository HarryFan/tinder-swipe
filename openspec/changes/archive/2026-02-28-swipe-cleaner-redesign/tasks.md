## 1. Application Theming

- [x] 1.1 Change the generic dark background (`bg-gray-900`) in `app/index.tsx` to a clean light background (`bg-slate-50`).
- [x] 1.2 Change empty state texts and general typography in `app/index.tsx` to a darker slate color (`text-slate-500` and `text-slate-800`).

## 2. Card Visual Identity (tinder-card.tsx)

- [x] 2.1 Remove hard borders and replace with a modern blurred drop shadow (`shadow-md shadow-black/10`).
- [x] 2.2 Change the internal background color of the card to `bg-white` rounding the corners nicely (`rounded-3xl` or `rounded-2xl`).

## 3. Action Feedback Overlay Styling (tinder-card.tsx)

- [x] 3.1 Update the "KEEP" positive feedback overlay from harsh green text to a modern translucent Teal or Indigo badge (`bg-teal-500/80`).
- [x] 3.2 Update the "DELETE" negative feedback overlay from harsh red text to a modern translucent Rose badge (`bg-rose-500/80`).

## 4. Bottom Controls (tinder-swipe.tsx)

- [x] 4.1 Update the Undo button to use the new modern color palette instead of default dark grays.
- [x] 4.2 Update the "Execute Deletions" button background and text color to match the Blue/White tech aesthetic (e.g., `bg-blue-600` or `bg-indigo-600` for primary button emphasis).
- [x] 4.3 Update the total available media batch counter label text to contrast against the new light background.
